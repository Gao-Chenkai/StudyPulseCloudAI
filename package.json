{
  "name": "studypulse-cloud-ai",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler d1 migrations apply StudyPulseDB --remote && wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.4",
    "vitest": "~3.2.0",
    "wrangler": "^4.114.0"
  }
}
